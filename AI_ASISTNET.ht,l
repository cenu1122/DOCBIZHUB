<!DOCTYPE html>
<html lang="hr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AI Glasovni Asistent - Pametni Outbound Pozivi</title>
    <style>
        :root {
            --primary: #4f46e5;
            --primary-hover: #4338ca;
            --bg-color: #0f172a;
            --card-bg: #1e293b;
            --text-main: #f8fafc;
            --text-muted: #94a3b8;
            --accent: #22c55e;
        }
        
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        body {
            background-color: var(--bg-color);
            color: var(--text-main);
            line-height: 1.6;
            padding: 20px;
        }

        .container {
            max-width: 1000px;
            margin: 0 auto;
            padding: 40px 20px;
        }

        header {
            text-align: center;
            margin-bottom: 50px;
        }

        header h1 {
            font-size: 2.5rem;
            margin-bottom: 15px;
            background: linear-gradient(to right, #818cf8, #c084fc);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        header p {
            font-size: 1.1rem;
            color: var(--text-muted);
        }

        .pricing-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 30px;
            margin-bottom: 50px;
        }

        .card {
            background-color: var(--card-bg);
            border-radius: 12px;
            padding: 30px;
            box-shadow: 0 10px 25px rgba(0,0,0,0.3);
            border: 1px solid #334155;
            display: flex;
            flex-direction: column;
            position: relative;
            transition: transform 0.3s ease;
        }

        .card:hover {
            transform: translateY(-5px);
        }

        .card.popular {
            border: 2px solid var(--primary);
        }

        .badge {
            position: absolute;
            top: -12px;
            right: 20px;
            background-color: var(--primary);
            color: white;
            padding: 4px 12px;
            font-size: 0.8rem;
            border-radius: 20px;
            font-weight: bold;
        }

        .card h3 {
            font-size: 1.5rem;
            margin-bottom: 10px;
        }

        .price {
            font-size: 2.2rem;
            font-weight: bold;
            color: var(--text-main);
            margin-bottom: 20px;
        }

        .price span {
            font-size: 1rem;
            color: var(--text-muted);
            font-weight: normal;
        }

        .features {
            list-style: none;
            margin-bottom: 30px;
            flex-grow: 1;
        }

        .features li {
            margin-bottom: 12px;
            color: var(--text-muted);
            display: flex;
            align-items: center;
        }

        .features li::before {
            content: "✓";
            color: var(--accent);
            font-weight: bold;
            margin-right: 10px;
        }

        .btn {
            background-color: var(--primary);
            color: white;
            border: none;
            padding: 12px;
            border-radius: 8px;
            font-size: 1rem;
            font-weight: bold;
            cursor: pointer;
            transition: background-color 0.2s;
            text-align: center;
            text-decoration: none;
        }

        .btn:hover {
            background-color: var(--primary-hover);
        }

        .usage-note {
            background-color: var(--card-bg);
            border-left: 4px solid var(--accent);
            padding: 20px;
            border-radius: 0 12px 12px 0;
            margin-top: 30px;
            color: var(--text-muted);
        }

        .usage-note h4 {
            color: var(--text-main);
            margin-bottom: 5px;
        }

        footer {
            text-align: center;
            margin-top: 50px;
            color: var(--text-muted);
            font-size: 0.9rem;
        }
    </style>
</head>
<body>

<div class="container">
    <header>
        <h1>AI Glasovni Asistent za Vaše Poslovanje</h1>
        <p>Automatizujte izlazne (outbound) pozive, prodaju i podršku uz naprednu AI tehnologiju.</p>
    </header>

    <div class="pricing-grid">
        <!-- Paket 1 -->
        <div class="card">
            <h3>Starter Asistent</h3>
            <div class="price">10$ <span>/ mjesecno</span></div>
            <ul class="features">
                <li>Osnovni AI model za razgovore</li>
                <li>Automatsko biranje brojeva</li>
                <li>Baza podataka i logovanje poziva</li>
                <li>Webhooks integracija</li>
            </ul>
            <a href="#kontakt" class="btn">Izaberi Starter</a>
        </div>

        <!-- Paket 2 -->
        <div class="card popular">
            <div class="badge">PREPORUČENO</div>
            <h3>Pro Business</h3>
            <div class="price">20$ <span>/ mjesecno</span></div>
            <ul class="features">
                <li>Napredni AI asistent (Brzi odziv)</li>
                <li>Detekcija sekretarice (AMD)</li>
                <li>Snimanje razgovora i audio URL-ovi</li>
                <li>Prioritetna podrška i stabilnost</li>
            </ul>
            <a href="#kontakt" class="btn">Izaberi Pro</a>
        </div>
    </div>

    <div class="usage-note">
        <h4>Fleksibilna potrošnja minuta</h4>
        <p>Pored mjesečne pretplate za održavanje sistema, troškovi samog razgovora obračunavaju se po utrošenom vremenu i iznose samo <strong>0,005$ po minuti</strong> razgovora. Plaćate tačno onoliko koliko razgovarate!</p>
    </div>

    <footer>
        <p>&copy; 2026 AI Asistent Sistemi. Sva prava zadržana.</p>
    </footer>
</div>

</body>
</html>
